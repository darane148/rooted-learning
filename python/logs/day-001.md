## Day 001 – 2026-04-04

**Focus:** Building a Python port scanner from scratch
**Environment:** Kali Linux 2024.1, Python 3.11, target: Metasploitable2 (192.168.56.101)

---

### The Code

```python
#!/usr/bin/env python3
"""
port_scanner.py — A simple multi-threaded TCP port scanner
Usage: python3 port_scanner.py <host> <start_port> <end_port>
"""

import socket
import sys
import threading
from datetime import datetime

# Store open ports for final summary
open_ports = []
lock = threading.Lock()


def scan_port(host: str, port: int) -> None:
    """Attempt a TCP connection to host:port. Print if open."""
    try:
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.settimeout(1)
        result = sock.connect_ex((host, port))
        sock.close()

        if result == 0:
            try:
                service = socket.getservbyport(port)
            except OSError:
                service = "unknown"

            with lock:
                open_ports.append(port)
                print(f"  [+] Port {port:5d}/tcp  OPEN  ({service})")

    except socket.error:
        pass


def main():
    if len(sys.argv) != 4:
        print(f"Usage: {sys.argv[0]} <host> <start_port> <end_port>")
        sys.exit(1)

    host = sys.argv[1]
    start_port = int(sys.argv[2])
    end_port = int(sys.argv[3])

    # Resolve hostname to IP
    try:
        target_ip = socket.gethostbyname(host)
    except socket.gaierror:
        print(f"[-] Cannot resolve host: {host}")
        sys.exit(1)

    print("-" * 50)
    print(f"Scanning target: {host} ({target_ip})")
    print(f"Port range:      {start_port} - {end_port}")
    print(f"Started at:      {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print("-" * 50)

    threads = []
    for port in range(start_port, end_port + 1):
        t = threading.Thread(target=scan_port, args=(target_ip, port))
        threads.append(t)
        t.start()

        # Throttle: max 100 concurrent threads
        if len(threads) >= 100:
            for t in threads:
                t.join()
            threads = []

    # Wait for remaining threads
    for t in threads:
        t.join()

    print("-" * 50)
    print(f"Scan complete. {len(open_ports)} open port(s) found.")
    print(f"Open ports: {sorted(open_ports)}")


if __name__ == "__main__":
    main()
```

---

### Running It

```bash
# Make executable
chmod +x port_scanner.py

# Scan top ports on Metasploitable2
python3 port_scanner.py 192.168.56.101 1 1000
```

**Output:**
```
--------------------------------------------------
Scanning target: 192.168.56.101 (192.168.56.101)
Port range:      1 - 1000
Started at:      2026-04-04 14:32:11
--------------------------------------------------
  [+] Port    21/tcp  OPEN  (ftp)
  [+] Port    22/tcp  OPEN  (ssh)
  [+] Port    23/tcp  OPEN  (telnet)
  [+] Port    25/tcp  OPEN  (smtp)
  [+] Port    80/tcp  OPEN  (http)
  [+] Port   139/tcp  OPEN  (netbios-ssn)
  [+] Port   445/tcp  OPEN  (microsoft-ds)
--------------------------------------------------
Scan complete. 7 open port(s) found.
Open ports: [21, 22, 23, 25, 80, 139, 445]
```

Results match nmap — scanner is working correctly.

---

### Mistakes Made

1. **First version used a single thread** — scanning 1000 ports sequentially with 1s timeout = up to 1000 seconds. Multi-threading brought it down to ~15 seconds.
2. **Race condition on `open_ports.append()`** — two threads appended at the same time and data got corrupted. Fixed by adding a `threading.Lock()`.
3. **No timeout on `socket.connect_ex()`** — closed ports caused the socket to hang waiting for a response that never came. Added `sock.settimeout(1)`.
4. **`getservbyport()` throws `OSError` for uncommon ports** — caught the exception and defaulted to `"unknown"`.

---

### What I Learned

- **`socket.connect_ex()`** returns 0 on success (port open), non-zero on failure. Better than `connect()` which throws an exception on failure.
- **Threading** is essential for a fast scanner — sequential scanning is O(n * timeout). With threads, you parallelize the wait time.
- **Race conditions** are real — any shared mutable state (`open_ports` list) must be protected by a lock when multiple threads write to it.
- **`socket.getservbyport()`** maps port numbers to service names using `/etc/services`. Returns "ftp" for 21, "ssh" for 22, etc.
- This is essentially what nmap does — just much simpler and without all the advanced features.

---

### Script Location

Save as `python/scripts/port_scanner.py`

---

### Next Steps

- Add banner grabbing (connect to port, read the first line of response)
- Add `-oN` output option to save results to a file
- Try building an HTTP directory brute-forcer using the `requests` library
